<UserControl
    x:Class="AdaptiveShell.UserControls.LiveTilesLayoutControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdaptiveShell.UserControls"
    xmlns:liveTiles="using:AdaptiveShell.LiveTiles.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:userControls="using:AdaptiveShell.LiveTiles.UserControls"
    mc:Ignorable="d">

    <Grid>
        <ProgressRing IsActive="{x:Bind IsLoading}" />

        <GridView
            CanDragItems="True"
            CanReorderItems="True"
            AllowDrop="True"
            ItemsSource="{x:Bind ItemsSource}">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="liveTiles:LiveTileModel">
                    <Grid>
                        <userControls:LiveTile AppId="{Binding AppId}" />
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</UserControl>
