{
    "$schema": "https://turbo.build/schema.json",
    "ui": "stream",
    "globalDependencies": [
        "**/.env.*local",
        "tsconfig.json",
        "**/tsconfig.json",
        "package.json",
        "**/package.json",
        ".env",
        "**/.env",
        "*.env",
        "**/*.env"
    ],
    "tasks": {
        "test": {
            "cache": false
        },
        "start": {
            "cache": false,
            "persistent": true,
            "inputs": ["$TURBO_DEFAULT$", ".env.development.local", ".env.local", ".env.development", ".env"]
        },
        "build": {
            "cache": true,
            "outputLogs": "new-only",
            "dependsOn": ["^build"],
            "outputs": [".tsup/**", ".expo/**", "!.expo/cache/**", "dist/**", "ios/**", "android/**"],
            "inputs": [
                "$TURBO_DEFAULT$",
                "!README.md",
                ".env.production.local",
                ".env.local",
                ".env.production",
                ".env"
            ]
        },
        "lint": {
            "cache": false
        },
        "typecheck": {
            "cache": false
        },
        "prettier": {},
        "format:eslint": {},
        "format:prettier": {},
        "docs": {
            "cache": true,
            "outputs": ["docs/**"],
            "dependsOn": ["^docs"],
            "inputs": ["$TURBO_DEFAULT$", "!README.md", "typedoc.json", "**/typedoc.json"]
        }
    }
}
